[
  {
    "task": "Parse an AWS S3 bucket name from an S3 URI (e.g., 's3://my-bucket/path/to/file.txt') and extract just the bucket name",
    "format": "regex"
  },
  {
    "task": "Create a JSON CloudFormation template snippet that defines an AWS Lambda function with basic properties including FunctionName, Runtime, Handler, and Role",
    "format": "json"
  },
  {
    "task": "Write a Python function that takes an AWS IAM policy JSON string and returns a list of all the Action values specified in the policy",
    "format": "python"
  }
]